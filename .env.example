# 豆包错题测评 - 配置示例
# 复制本文件为 .env (或直接设置系统环境变量)，按需填写。

# ===== 判卷模型 B (可选；不填则默认用智谱 GLM 视觉模型) =====
# 想换成判负号更准的视觉模型？填下面四项即可(需兼容 OpenAI chat/completions 格式)：
# 例如 GPT-4o / Claude / Gemini：
# B_API_URL=https://api.openai.com/v1/chat/completions
# B_API_KEY=sk-你的key
# B_MODEL=gpt-4o
# B_FALLBACK=gpt-4o-mini

# ===== 智谱 GLM (默认 B 模型) =====
# 不填 B_API_* 时，会从 ~/.claude.json 读已配的智谱 key；也可显式指定：
# ZHIPU_API_KEY=你的智谱key

# ===== A(豆包) =====
# 豆包用网页自动化，无需 API key；首次运行需手动登录一次(登录态存 browser_data/)。
